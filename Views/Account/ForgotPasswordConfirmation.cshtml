<h2>Check Your Email</h2>
<p>A password reset link has been sent.</p>

@if (ViewBag.Link != null)
{
    <p>
        <strong>Dev Link:</strong><br />
        <a href="@ViewBag.Link" style="word-break:break-all;">@ViewBag.Link</a>
    </p>
}
<a asp-action="Login" class="btn btn-primary">Back to Login</a>